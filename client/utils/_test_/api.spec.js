import { signIn } from '../api';
describe('signIn()', () => {
    it('Should return correct structure', async () => {

    })
    it('Should contain the body of MISSING_EMAIL if code is not have the email field from user', () => {

    })
});